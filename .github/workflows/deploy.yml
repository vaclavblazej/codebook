name: Build and Deploy

on:
  push:
    branches:
      - main

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build Rust
        run: |
          cd make
          cargo build --release

      - name: Run Rust Project to Generate Content
        run: |
          cd make
          ./target/release/codebook

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: '0.145.0'

      - name: Build Hugo Site
        run: |
          cd web
          hugo --minify

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
